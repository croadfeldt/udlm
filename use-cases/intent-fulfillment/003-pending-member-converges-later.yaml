uuid: uc-7cdec1fb-c656-4d19-938d-b08f22b718b0
handle: intent-fulfillment/pending-member-converges-later
version: 1.0.0
scenario:
  description: A best_effort intent left a member pending for transient capacity (UC-001). Later, capacity
    frees. The reconciliation loop realizes the previously-pending member with no new consumer request
    — the original intent was persistent desired state, not a one-shot command. The consumer's surfaced
    state updates from pending to realized, and the shortfall warning clears.
  actor:
    persona: platform-engineer
    profile: standard
  intent: Have a persistent intent's pending member converge to Realized later via reconciliation, with
    no re-request and the surfaced state updating
  success_criteria:
  - The pending member realizes when its transient blocker clears, driven by reconciliation, not a new
    request
  - The original intent persisted as desired state across the gap (Intent held, not discarded)
  - The consumer's surfaced shortfall clears when the member reaches Realized
  - No duplicate or conflicting realization results from the deferred convergence
  - The convergence event is auditable back to the original intent
  dimensions:
    lifecycle_phase: day2_operations
    resource_complexity: composite_service
    policy_complexity: single_validation
    provider_landscape: multiple_eligible
    governance_context: standard_governance
    failure_mode: happy_path
  profile: standard
  expected_domain_interactions:
  - domain: data
    interaction: Intent persists as desired state; realized state is its current projection
  - domain: policy
    interaction: reconciliation converges the pending member without re-request
  - domain: provider
    interaction: capacity frees; reserve+realize the held member
  - domain: audit
    interaction: convergence traces to the original intent
generated_by:
  mode: authoring
  source: human-authored
  model: null
  prompt_version: intent-fulfillment-2026-07-27
  timestamp: '2026-07-27T16:00:00Z'
tags:
- hammer
- intent-fulfillment
- convergence
- desired-state
- reconciliation
