uuid: uc-c22d117e-6a3b-4805-bbcd-c897a0fa2b5e
handle: intent-fulfillment/dependents-of-refused-member-held-not-dangling
version: 1.0.0
scenario:
  description: 'A member A is pending for a TRANSIENT reason (capacity). Member B hard-depends on A. B
    is not realized dangling and not failed outright — it is HELD pending, its state tracking A''s: while
    A converges, B waits; if A realizes, B proceeds; if A''s blocker turns permanent, B''s classification
    follows to refused. Dependents inherit their dependency''s classification rather than being realized
    against something that isn''t there.'
  actor:
    persona: platform-engineer
    profile: standard
  intent: Have dependents of an unrealized member inherit its classification — held while transient, refused
    if it becomes permanent — never realized dangling
  success_criteria:
  - A dependent of a pending member is held pending, not realized and not failed
  - The dependent's classification tracks its dependency's (transient->held, permanent->refused)
  - The dependent never realizes while its hard dependency is unrealized
  - When the dependency realizes, the dependent proceeds automatically (convergence)
  - The dependency chain's state is surfaced coherently — the consumer sees why B waits (on A)
  dimensions:
    lifecycle_phase: day2_operations
    resource_complexity: hard_dependencies
    policy_complexity: multiple_interacting
    provider_landscape: multiple_eligible
    governance_context: standard_governance
    failure_mode: partial_fulfillment
  profile: standard
  expected_domain_interactions:
  - domain: data
    interaction: dependent classification derives from dependency classification
  - domain: policy
    interaction: held-vs-refused inherited along the hard-dependency edge
  - domain: provider
    interaction: dependent reserved/realized only after its dependency
  - domain: audit
    interaction: the dependency chain and inherited states are recorded
generated_by:
  mode: authoring
  source: human-authored
  model: null
  prompt_version: intent-fulfillment-2026-07-27
  timestamp: '2026-07-27T16:00:00Z'
tags:
- hammer
- intent-fulfillment
- dependents
- classification-inheritance
- convergence
