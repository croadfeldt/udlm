# Built-in profile: FSI — financial-services / regulated posture.
# policy_profile grouping (DCM ADR-007). Superset of prod: tamper-evident (merkle) audit,
# attestation-gated providers, regulatory retention, governance-matrix on ALL lifecycle operations,
# and the override-approval workflow. Full production architecture — implemented against dev in Sept.
record_type: profile
uuid: 6c86370c-0b54-4417-bd60-6359c748d1e5
handle: udlm/profile/fsi
conforms_to: udlm/0.1
name: FSI (Financial Services)
description: Regulated financial-services profile — the prod floor plus tamper-evident Merkle audit with
  inclusion/consistency proofs (universal-audit §8, RFC 9162), attestation-gated provider admission (accreditation
  matrix; DCM ADR-022), regulatory retention, governance-matrix evaluated on every lifecycle operation
  (no scope-out), and the time-bounded override-approval workflow (policy-contract §18). Architecture
  is production-grade; September implements it against the dev floor for co-engineering.
version: 1.1.0
status:
  state: active
contains:
- ref: estate/policy/structural-validation
  state: required
- ref: estate/policy/tenant-isolation
  state: required
- ref: estate/policy/resolved-profile-evaluation
  state: required
- ref: estate/policy/governance-matrix-all-operations
  state: required
- ref: estate/policy/recovery
  state: required
- ref: estate/policy/drift-reconciliation
  state: required
- ref: estate/policy/blast-radius-impact
  state: required
- ref: estate/policy/dual-approval-destructive
  state: required
- ref: estate/policy/override-approval
  state: required
- ref: estate/audit/merkle-transparency-log
  state: required
- ref: estate/lifecycle/four-state-tracking
  state: required
- ref: estate/store/requested
  state: required
- ref: estate/store/realized
  state: required
- ref: estate/store/discovered
  state: required
- ref: estate/time-sync/attested
  state: required
- ref: estate/discovery/scheduled
  state: required
- ref: estate/recovery/bounded-convergence
  state: required
- ref: estate/audit/merkle-log
  state: required
- ref: estate/accreditation/attestation
  state: required
- ref: estate/accreditation/verifiable
  state: required
- ref: estate/retention/regulatory
  state: required
activation:
  is_builtin: true
  default: false
  approved: true
