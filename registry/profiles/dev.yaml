# Built-in profile: DEVELOPMENT / EVALUATION — the September implementation target.
# A profile record (registry/profile.schema.json; DCM ADR-007 — a profile is a composed
# SET with a floor, NOT a level). Relaxed floor for co-engineering/evaluation; the architecture and
# wire contracts are identical to the production profiles — only the required floor differs.
record_type: profile
uuid: cf28544a-6d7e-46d6-ae3f-ddb1c07a354e
handle: udlm/profile/dev
conforms_to: udlm/0.1
name: Development / Evaluation
description: Baseline profile for development, evaluation, and co-engineering — the September implementation
  target. Minimal required floor so the 21 release use cases can be exercised end to end without production-grade
  governance ceremony. Same architecture, types, and wire contracts as prod/fsi/sovereign; fewer required
  mechanics.
version: 1.1.0
status:
  state: active
contains:
- ref: estate/policy/structural-validation
  state: required
- ref: estate/policy/tenant-isolation
  state: required
- ref: estate/policy/resolved-profile-evaluation
  state: required
- ref: estate/audit/append-only-log
  state: required
- ref: estate/lifecycle/four-state-tracking
  state: required
- ref: estate/store/requested
  state: required
- ref: estate/store/realized
  state: required
- ref: estate/store/discovered
  state: required
- ref: estate/time-sync/causal
  state: required
activation:
  is_builtin: true
  default: true
  approved: true
