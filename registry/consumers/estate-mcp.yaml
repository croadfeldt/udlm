# Consumer manifest (ADR-044) — self-reported read surface; gated by
# tests/check_consumer_conformance.py.
#
# estate-mcp serves the estate's UDLM records to agent tooling read-only; like estate CI its
# surface is the record envelope, so any registry type can flow through it.
consumer:
  name: estate-mcp
  repo: private estate tooling (GitLab, kept off GitHub)  # TODO verify — confirm the canonical repo location with the estate maintainer
  description: MCP server exposing the estate's UDLM records (read-only) to agent tooling.
consumes_all_types: true
coverage: declared
