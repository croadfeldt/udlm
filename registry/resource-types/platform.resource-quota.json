{
  "$id": "https://udlm.dev/registry/udlm/0.1/Platform.ResourceQuota/0.3.0",
  "conforms_to": "udlm/0.1",
  "uuid": "d7e48f39-2b56-4a8c-9c1d-5f6a0b7e3d12",
  "resource_type": "Platform.ResourceQuota",
  "version": "0.3.0",
  "family": "Resource",
  "status": "active",
  "metadata": {
    "display_name": "Resource Quota",
    "description": "A resource quota scoped to a namespace — hard limits on aggregate resource consumption (CPU, memory, pods, storage, object counts) within a single namespace. Models the Kubernetes ResourceQuota construct. Placement and enrichment policies query quotas to determine whether a namespace has capacity for a new workload before dispatching. Discovered-state reporting surfaces actual usage against the hard limits for capacity planning and drift detection.",
    "maintainer": "udlm-registry",
    "origination_timestamp": "2026-07-17T00:00:00Z"
  },
  "context": {
    "purpose": "Declares hard consumption limits for one namespace so capacity questions are answerable before a workload is dispatched.",
    "plain_description": "The Kubernetes ResourceQuota construct as a record: aggregate CPU, memory, pod count, storage, and object-count ceilings for a single namespace. Discovered usage is reported against the limits, so whether a namespace has room for a request is a data question a placement policy answers up front — and quota pressure is visible for capacity planning.",
    "use_when": [
      "You need per-namespace ceilings on aggregate consumption enforced and visible.",
      "You need admission or placement to check remaining headroom before dispatching a workload."
    ],
    "not_for": [
      "Per-container resource requests — those live on the workload (Compute.Container resources).",
      "Node capacity — Platform.NodePool advertises capacity; a quota caps consumption within a namespace."
    ],
    "works_with": [
      "Platform.Namespace — the one namespace this quota constrains.",
      "Compute.Container — workloads whose aggregate consumption the quota caps."
    ]
  },
  "adopts": [
    {
      "standard": "Kubernetes",
      "version": "core/v1",
      "standard_name": "ResourceQuota",
      "role": "namespace-scoped aggregate resource limits and usage tracking",
      "source": "https://kubernetes.io/docs/concepts/policy/resource-quotas/",
      "license": "CC-BY-4.0",
      "license_compatibility": "compatible-reference",
      "disposition": "tier1_value"
    }
  ],
  "lifecycle": {
    "states": [
      "Intent",
      "Requested",
      "Realized",
      "Discovered"
    ],
    "decommission": "Provider removes the quota. Resources in the namespace are no longer constrained by this quota (other quotas may still apply)."
  },
  "spec": {
    "type": "object",
    "required": [
      "namespace_ref",
      "hard"
    ],
    "properties": {
      "namespace_ref": {
        "description": "Resource reference to a Platform.Namespace (common-elements §2.5, ADR-025). Authored by handle; deferred resolution supported (claim-before-define).",
        "oneOf": [
          {
            "type": "string",
            "description": "Short form — the handle of an existing Platform.Namespace."
          },
          {
            "$ref": "https://udlm.dev/registry/udlm/0.1/common-elements.schema.json#/$defs/Reference"
          }
        ]
      },
      "hard": {
        "type": "object",
        "description": "Hard resource limits. Any key is a Kubernetes resource name.",
        "properties": {
          "cpu": {
            "type": "string",
            "description": "Aggregate CPU limit (e.g., '32', '16000m')."
          },
          "memory": {
            "type": "string",
            "description": "Aggregate memory limit (e.g., '64Gi')."
          },
          "pods": {
            "type": "integer",
            "description": "Maximum number of pods."
          },
          "requests.storage": {
            "type": "string",
            "description": "Aggregate storage request limit."
          },
          "persistentvolumeclaims": {
            "type": "integer",
            "description": "Maximum number of PVCs."
          }
        },
        "additionalProperties": {
          "type": "string"
        }
      }
    }
  },
  "outputs": {
    "used": {
      "type": "object",
      "description": "Current resource usage against the hard limits (discovered state). Same keys as hard limits."
    }
  },
  "relationships": [
    {
      "edge_type": "contained_by",
      "target": "Platform.Namespace",
      "cardinality": "1..1",
      "name": "namespace",
      "description": "The namespace this quota constrains."
    }
  ]
}
