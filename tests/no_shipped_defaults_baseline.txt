# UDLM-shipped defaults — burn-down baseline
#
# EMPTY, and it stays that way. The 46 sites present when NDF-001 landed were removed in the same
# change: Data.Database.version "latest", Storage.FileShare.protocol "smb",
# Storage.Volume.volume_mode "filesystem", and 43 others. Each was one organisation's opinion baked
# into a portable type, arriving with no provenance.
#
# A default belongs in a layer or a profile, where the value records which layer set it and policy
# can override it traceably. An opinionated implementation ships a layer.
#
# This file exists so the ratchet has a home if a default is ever knowingly accepted. Adding a line
# means accepting debt — say why in the same change.
